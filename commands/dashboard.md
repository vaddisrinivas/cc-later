---
name: dashboard
description: Generate and open the cc-later dashboard in your browser. Shows queue health, window status, dispatch timeline, agent results, and failure patterns.
user_invocable: true
---

```bash
uv run --project ${CLAUDE_PLUGIN_ROOT} python3 ${CLAUDE_PLUGIN_ROOT}/scripts/dashboard.py
```
