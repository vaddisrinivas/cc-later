---
name: stats
description: Show cc-later analytics dashboard — success rates, token usage, trends by model and repo.
---

Run the following command and display the output:

```bash
python3 ${CLAUDE_PLUGIN_ROOT}/cc_later/cli.py stats
```
